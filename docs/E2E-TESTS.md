# E2E Tests (Playwright)

The Playwright suite is documented as part of the broader testing strategy — Vitest unit tests, Playwright e2e, and Storybook — in a single file: [TESTING.md](./TESTING.md).

- **Commands, config, coverage matrix, project split (desktop / mobile), CI setup, environment for real OneEntry runs** — [TESTING.md §2](./TESTING.md#2-e2e-tests--playwright).
- **Full test-file inventory (302 tests across 12 specs + `helpers.ts`)** — same section.
- **Shared helpers** — [`e2e/helpers.ts`](../e2e/helpers.ts).

This file remains as a redirect so links in older docs / commits continue to work.
